#include<bits/stdc++.h>
using namespace std;
int factorial(int n){
    int fact;
    fact=1;
    for(int i=1;i<=n;++i){
        fact=fact*i;
    }
    return fact;
}
int main(){
    int a;
    int factorials;
    cin>>a;
    factorials=factorial(a);
    cout<<factorials;
}
